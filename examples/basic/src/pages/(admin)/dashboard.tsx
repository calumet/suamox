import { Head } from '@suamox/head';

export default function DashboardPage() {
  return (
    <div>
      <Head>
        <title>Suamox - Dashboard</title>
        <meta name="description" content="Admin dashboard example." />
      </Head>
      <h1>Dashboard</h1>
      <p>Admin dashboard - route group example</p>
    </div>
  );
}
